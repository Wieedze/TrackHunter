/**
 * Lightweight HTML parsing utilities for Cloudflare Workers.
 * Uses HTMLRewriter (cf-htmlrewriter) instead of Cheerio.
 *
 * TODO: Implement extraction helpers.
 */

export function extractText(html: string, selector: string): string[] {
  // Placeholder — will use HTMLRewriter API in actual implementation
  void html;
  void selector;
  return [];
}
